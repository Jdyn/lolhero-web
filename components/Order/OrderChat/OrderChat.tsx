import React, { useState } from 'react';
import styles from './styles.css';

// interface Props {}

const OrderChat = (props): JSX.Element => {
  return (
    <div className={styles.root}>
      <h3>Chat</h3>
    </div>
  );
};

export default OrderChat;
